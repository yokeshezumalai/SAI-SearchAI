# Transaction Search API Environment Variables

# Server Configuration
HOST=0.0.0.0
PORT=8000
DEBUG=false

# Data Directory (where Milvus Lite stores data)
TRANSACTION_DATA_DIR=./data

# Optional: Milvus Server (if not using Milvus Lite)
# MILVUS_URI=http://localhost:19530
# MILVUS_USER=root
# MILVUS_PASSWORD=Milvus
